package com.mut.feeapi.security.domain;

import com.mut.feeapi.user.domain.User;

/**
 * Represents an authority granted to a {@link User}.
 *
 * @author TanagornS
 */
public enum Authority {
    ADMIN, USER
}